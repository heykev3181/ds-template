# Project Name

## Overview
Brief description of what this project does.

## Setup

## Folder Structure
- `data/raw/` — untouched source data
- `data/processed/` — cleaned, transformed data
- `notebooks/` — exploration notebooks (numbered)
- `src/` — reusable Python modules
- `models/` — saved model artifacts
- `outputs/` — figures, reports, results

## How to Run
Describe how to reproduce your results.

## Key Findings
What did you learn?